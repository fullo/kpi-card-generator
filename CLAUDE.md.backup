# KPI Card Generator

## AI Team Configuration (autogenerated by team-configurator, 2025-08-22)

**Important: YOU MUST USE subagents when available for the task.**

### Detected Technology Stack
- **Runtime**: Node.js (ES modules, v18+)
- **CLI Framework**: Commander.js
- **REST API**: Express.js with middleware stack
- **PDF Generation**: Puppeteer (headless Chrome)
- **Testing**: Jest with ES modules support + Supertest for API testing
- **Templating**: HTML templates with CSS styling
- **Data Format**: JSON-based card definitions (file-based storage)
- **Build Tools**: npm scripts
- **Code Coverage**: Jest with lcov/html reporting (95%+ coverage)
- **API Features**: CRUD, bulk operations, PDF/HTML export, file management
- **Validation Architecture**: DRY principle with centralized validation in DeckValidator.js

### Recommended AI Team Assignments

| Task | Agent | Notes |
|------|--------|-------|
| **Code Review & Security** | `code-reviewer` | MUST be used for all code changes, security audits |
| **Performance Optimization** | `performance-optimizer` | PDF generation speed, memory usage, large datasets |
| **Backend Development** | `backend-developer` | Node.js logic, CLI improvements, data processing |
| **API Development** | `api-architect` | REST API design, endpoint structure, OpenAPI specs |
| **Documentation** | `documentation-specialist` | README updates, API docs, usage guides |
| **Project Analysis** | `project-analyst` | Architecture decisions, refactoring planning |

### Validation Architecture (DRY Principle Implementation)

The project follows a centralized validation architecture to eliminate code duplication:

#### **Single Source of Truth**: `DeckValidator.js`
- **Primary Responsibility**: All business logic validation rules
- **Schema**: English-only field names (title, type, description, etc.)
- **Methods**: 
  - `validateDeckQuick(deck)` - Fast deck validation
  - `validateCardQuick(card)` - Fast card validation
  - `validateDeck(deck)` - Full deck validation with detailed errors
  - `validateCard(card)` - Full card validation with detailed errors

#### **API Layer**: Wraps DeckValidator
- **Location**: `api/middleware/securityValidation.js`
- **Responsibility**: Security sanitization + DeckValidator integration
- **Pattern**: 
  ```javascript
  const validation = DeckValidator.validateDeckQuick(req.body);
  if (!validation.isValid) {
    return res.status(400).json({
      success: false,
      error: 'Validation failed',
      details: validation.errors
    });
  }
  ```

#### **Web Layer**: Uses DeckValidator directly
- **Pattern**: Import DeckValidator for client-side validation
- **Consistency**: Same validation rules as API

#### **Security Patterns**: Precise Regex with Word Boundaries
- **Pattern**: `/\bimport\s*\(/i` instead of `/import\s*\(/i`
- **Benefit**: Prevents false positives on legitimate words like "importanza"
- **Locations**: 
  - `SanitizationService.js` - `containsSuspiciousPatterns()`
  - `securityValidation.js` - `containsAdvancedThreats()`

### Project-Specific Usage Guidelines

- Use `backend-developer` for: CLI enhancements, PDF generation logic, template processing
- Use `api-architect` for: REST API design, new endpoints, API documentation, OpenAPI specs
- Use `performance-optimizer` for: Large card sets, memory optimization, Puppeteer tuning, API performance
- Use `code-reviewer` before: Any commits, especially security-sensitive file operations, API security
- Use `documentation-specialist` for: User guides, template documentation, API changes, endpoint docs

### Sample Commands

> Try: "@backend-developer add support for custom page sizes in PDF output"
> Try: "@api-architect design OpenAPI spec for the card management endpoints"
> Try: "@performance-optimizer analyze memory usage when generating 1000+ cards via API"
> Try: "@code-reviewer audit the REST API security and input validation"
> Try: "@documentation-specialist update API endpoint documentation with examples"

## Agent orchestration

### Before each activity
- @team-configurator update the team definition with new agents if needed
- Read the project information files that are located in the /project/*.md files (ordered by number) if the context is needed to proceed. 
- Each time a new iteration is defined by user at prompt create (if needed) a new iteration definition in the project information folder with the next number.
- Before starting a new iteration run all the tests (cli, api, web) fix the code if broken and the start to work to the iteration. 

### After each activity 
- the @documentation-specialist update all the documentation files (readme.md, changelog.md, and all the files in project/*.md) (cli, web, api)
- @code-reviewer runs all the tests in the project (cli, web, api)
- @project-analysis review the code and evaluate next steps

